'use client';

import { ColumnDef } from '@tanstack/react-table';
import { UserProfile } from '@/lib/types';

// This component is no longer used. Returning an empty array for columns.
export const columns: ColumnDef<UserProfile>[] = [];
